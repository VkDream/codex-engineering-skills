# 从零开始搭建这套工作流

这套工作流由四部分组成：个性化指令约定怎么做事，知识库保存项目记录，Skill 提供各类工作的做法，六角色流程负责需要分工时的任务交接。

先把前三部分配置好，用小任务确认能正常工作，再决定是否启用六角色。下面的搭建过程需要在你自己的电脑上完成；下载文件不代表已经安装或启用。

## 1. 先确认文件放在哪里

准备好下载后的分享包，再确认三个位置：知识库目录、Skill 安装目录、实际生效的全局指令文件。把这些位置告诉 Codex，让它先检查已有内容，提出配置方案。

已有知识库或规则时，先比较、备份再合并。没有知识库时，先选一个空目录。知识库用来存放项目说明和工作记录，真实源码仍放在原来的工程目录。

路径占位符的填写方式见 [README](README.md)。其中 `{SKILLS_ROOT_URI}` 是 Skill 目录对应的 file URL，需要单独正确填写；四种占位符都不能留着直接使用。

## 2. 配置个性化指令

按 [个性化设置说明](个性化设置.md)，将 [全局指令分享版](rules/GLOBAL_AGENTS.md) 中适合自己的内容合并到实际生效的全局指令里。回答风格、模型和并发设置可以按需选择。

如果还没确定知识库和 Skill 的路径，先不要保存一份带有未替换标记的正式配置。

## 3. 建立知识库和第一个项目记录

将本包 `knowledge-base` 文件夹里的内容复制到你选定的知识库目录。注意复制的是文件夹里的内容，避免多套一层 `knowledge-base` 导致路径不匹配。

本包先提供三个起点：

| 文件 | 用途 |
|---|---|
| `AGENTS.md` | 告诉 Codex 进入知识库后先读什么 |
| `00_Governance/RULES.md` | 约定记录保存位置、更新方式和事实标准 |
| `01_Projects/示例项目/START_HERE.md` | 项目入口模板，使用前需要填写 |

知识库的完整目录分工如下。目录没有内容时，可以等实际需要时再创建。

```text
你的知识库目录/
├─ AGENTS.md
├─ 00_Governance/    知识库规则和治理记录
├─ 01_Projects/      各项目的说明、进度和任务记录
├─ 02_Knowledge/     多个项目可以复用的知识
├─ 03_Agent_Outputs/ 长报告及尚未整理的输出
└─ 99_Archive/       旧版本和归档记录
```

创建第一个项目时，在 `01_Projects` 下建立以该项目命名的目录，并参考“示例项目”填写 `START_HERE.md`。至少写清项目名称、真实源码路径、当前目标、已有进度、本次允许和禁止的操作，以及下一步。不确定的内容保留“待确认”，不要编造测试成绩或默认授权。

后续按需要补充项目文件：`PATHS.md` 记录位置，`STATUS.md` 记录当前状态，`TODO.md` 记录下一步，`RISKS.md` 记录已知风险。一次完整任务或里程碑的执行记录放在 `RUNS/年月/` 下，规则里称它为主 RUN；同一目标的修改、检查和返修放在同一份记录里。具体职责以 [知识库治理规则](knowledge-base/00_Governance/RULES.md) 为准，不必一开始就建满所有文件。

下面这段话用于请求搭建方案。将方括号替换为自己的信息后，发给 Codex：

```text
分享包目录：[填写下载目录]
计划使用的知识库目录：[填写目录；已有知识库请注明]
项目名称：[填写名称]
真实源码目录：[填写已经确认的路径]

请先检查已有配置，参考分享包提出搭建方案：
1. 哪些个性化指令需要合并，哪些路径需要替换；
2. 知识库需要复制或新建哪些文件；
3. 第一个项目的 START_HERE.md 应填写哪些已知事实；
4. 七个 Skill 应安装在哪里，怎样检查是否可用。
先只读检查，不修改文件。等我确认具体方案和写入范围后再配置。
不要移动源码、导入全部聊天或私人历史，也不要连接数据库、运行设备或对外发布。
```

## 4. 安装七个 Skill

完整复制本包 `skills` 下的七个文件夹：`codex-workflow-router` 和六个 `kb-role-*`。保留各文件夹中已有的子目录和文件，不要只复制 `SKILL.md`。

按 [OpenAI Skill 文档](https://developers.openai.com/codex/skills)，可以使用用户目录下的 `.agents/skills`，也可以使用项目内的 `.agents/skills`。选择实际适用的一处安装，避免同名 Skill 重复出现。如果使用了自定义路径，请先确认客户端能从那里加载。

检查当前会话可用的 Skill 列表。能在磁盘上找到文件，不等于当前会话已经识别它。文档说明 Skill 变更通常会自动识别；未出现时，再按客户端要求重新加载或重启。

## 5. 先检查配置，再用小任务试用

可以先要求 Codex 只读检查并报告：它读取了哪个全局指令文件，知识库入口是否存在，项目入口是否指向正确工程，以及七个 Skill 中哪些在当前会话可用。让它列出证据和未确认项，不要仅回答“配置成功”。

然后在明确允许的范围内完成一个小任务，检查它是否按规则读取了必要记录、围绕目标工作、做了相关检查，并把结果写回同一份任务记录。没有做过的验证，应如实列出。

这一步只说明你试过的那条路径可用。模型、外部工具或其他项目的完整运行情况，需要分别检查。

## 6. 需要分工时，再启用六角色

确认项目和任务目标后，可以明确要求：

```text
请为[已确认的项目名称和路径]启用六角色协作，当前目标是[填写一个完整目标]。
先检查当前客户端是否提供创建任务、读取任务和发送消息的工具。
工具可用时，按分享包的六角色协议建立固定角色任务，由总监确定范围和安排工作。
如果工具不可用，请说明缺少什么，不要声称角色已创建或自动交接已完成。
创建角色任务不代表已经授权修改工程、操作真实设备或对外发布；具体操作范围另行明确。
```

总监负责目标和安排，诊断负责查清问题，开发负责实现，审查负责独立检查，修复负责处理发现，验证负责检查约定结果。并非每个任务都要走完全部角色，也不需要让六个角色同时运行。

## 7. 平时怎么继续做项目

每次开始时说明当前项目和这次目标，让 Codex 先读项目入口，再按需读当前任务记录。结束时更新任务记录和有变化的项目状态，不要把每次工具调用都另存成一份任务。

换任务页时，把项目入口、当前主 RUN 和下一步一并交接；已经做过的内容也要写清楚，避免新任务重复执行。原任务没有验证的内容，不能因为换页就变成“已完成”。

公开分享时，只提交通用模板和说明。你后来填入的客户信息、真实项目记录、账号配置和运行日志，都应留在自己的环境里。
