# 我的 Codex 个性化设置

我使用的个性化设置分为两部分：一部分选择回答风格，另一部分约定 Codex 怎么做项目。

| 设置 | 我的分享内容 | 在哪里看 |
|---|---|---|
| 回答风格 | 本机保存的是 `pragmatic`（Pragmatic） | [可选配置示例](optional-config/config.example.toml) |
| 自定义指令 | 完整的项目工作约定，已去掉私人路径 | [个性化指令正文](rules/GLOBAL_AGENTS.md) |

`rules/GLOBAL_AGENTS.md` 就是我个人全局指令的分享版，不需要再找另一份“个性化提示词”。本页只解释用法，指令正文集中保存在那一个文件里。

## 在自己的 Codex 中怎么设置

打开 Codex 的设置，找到个性化（Personalization）。按照 [OpenAI 官方说明](https://learn.chatgpt.com/docs/reference/settings)，这里可以选择回答风格，也可以编辑自定义指令；修改自定义指令会更新个人的 `AGENTS.md` 文件。

回答风格可以选 Pragmatic，也可以按自己的习惯选择其他选项。配置示例里的 `personality = "pragmatic"` 只表示我本机保存的选择，不代表你的客户端已经应用，也不会替你切换模型。

自定义指令按下面的顺序处理：

1. 阅读 [指令正文](rules/GLOBAL_AGENTS.md)，确认其中的工作方式适合自己。
2. 按 [README 的路径说明](README.md) 替换四种路径占位符。知识库和 Skill 的实际位置要先确认，不能照抄作者电脑的路径。
3. 备份已有自定义指令，再比较、合并。请保存指令正文，不要把本页使用说明一起粘贴进去。
4. 在一个小任务中检查 Codex 是否读取了预期的指令文件。如果读到的是别的文件，先查明原因，再继续使用。

也可以直接维护全局 `AGENTS.md` 文件。其默认位置是用户目录下的 `.codex/AGENTS.md`；设置了 `CODEX_HOME` 时，以该目录为准。同一位置若存在非空的 `AGENTS.override.md`，Codex 会优先读取它。不要为了套用本包而直接删除已有覆盖文件。[文件查找规则](https://learn.chatgpt.com/docs/agent-configuration/agents-md)

## 哪些内容需要按自己的情况调整

- 知识库路径、Skill 路径和实际生效的全局指令路径。
- Git 的授权方式。本模板默认要求先明确授权，包括只读 Git 命令。
- 模型和并发设置。分享版保留我的参考选择，但使用者需要确认自己的客户端和账号是否支持。
- 项目里的具体限制。请按自己的项目填写，不要把示例项目当成已授权的真实工程。

分享版与我的本机指令保留相同的工作约定；私人目录已换成占位符，个人模型选择的措辞改为参考配置。本包没有包含账号凭据、聊天记录或整份本机 `config.toml`，也不提供 OpenAI 内置风格提示词的副本。

个性化设置只约定工作方式。知识库如何建立、Skill 如何安装，以及后续怎样开展任务，请继续看 [从零开始搭建工作流](从零开始.md)。
